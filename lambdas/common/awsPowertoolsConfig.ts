export const awsPowertoolsConfig = {
  namespace: 'aws-powertools-test',
};

export enum MetricNames {
  successes = 'Successes',
  failures = 'Failures',
}

export enum ServiceNames {
  metricsTest = 'metrics-test',
  tracerTest = 'tracer-test',
}
